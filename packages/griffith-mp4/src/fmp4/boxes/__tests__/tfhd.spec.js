import tfhd from '../tfhd'

describe('tfhd box', () => {
  it('should be same', () => {
    // prettier-ignore
    expect(tfhd({trackId: 2})).toEqual(new Uint8Array([
      0x00, 0x00, 0x00, 0x10,
      0x74, 0x66, 0x68, 0x64,
      0x00, 0x00, 0x00, 0x00,
      0x00, 0x00, 0x00, 0x02,
    ]))
  })
})
